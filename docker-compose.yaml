services:
  saj2mqtt:
    env_file: ".env"
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/home/node/saj2mqtt/src
    container_name: saj2mqtt
    expose:
      - "4000"
    ports:
      - "4000:4000"
    command: npm run start
